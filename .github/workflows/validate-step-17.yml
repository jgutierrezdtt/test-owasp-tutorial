name: Validate Step 17

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - src/typescript/src/search.controller.ts
      - .tutorial/state.env
      - .tutorial-started

jobs:
  validate:
    permissions:
      contents: write
    uses: ./.github/workflows/reusable-step.yml
    with:
      step_number: '17'