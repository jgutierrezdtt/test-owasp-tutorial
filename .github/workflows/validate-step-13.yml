name: Validate Step 13

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - src/go/handlers/search.go
      - .tutorial/state.env
      - .tutorial-started

jobs:
  validate:
    permissions:
      contents: write
    uses: ./.github/workflows/reusable-step.yml
    with:
      step_number: '13'