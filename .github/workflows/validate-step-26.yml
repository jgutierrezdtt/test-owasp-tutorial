name: Validate Step 26

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - src/go/handlers/orders.go
      - .tutorial/state.env
      - .tutorial-started

jobs:
  validate:
    permissions:
      contents: write
    uses: ./.github/workflows/reusable-step.yml
    with:
      step_number: '26'
