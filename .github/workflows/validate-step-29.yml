name: Validate Step 29

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - src/typescript/src/profile.controller.ts
      - .tutorial/state.env
      - .tutorial-started

jobs:
  validate:
    permissions:
      contents: write
    uses: ./.github/workflows/reusable-step.yml
    with:
      step_number: '29'
